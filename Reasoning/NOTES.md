# Reasoning — library index

**This file is superseded by [`GUIDE.md`](GUIDE.md).** See `GUIDE.md` for the per-module routing
table, the import layering, and the cross-cutting conventions/gotchas; see each `Reasoning/*.lean`
file's `/-! # … -/` header for per-module detail.

(The old contents of this file were stale — they predated the multi-function examples and the switch
to `native_decide` for decode discharge, and listed only a subset of the library modules.)
