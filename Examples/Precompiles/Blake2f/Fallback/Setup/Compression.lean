import Examples.Precompiles.Blake2f.Fallback.Setup.Compression.Rounds

/-!
# BLAKE2F fallback compression setup traces

Aggregator for t parsing, vector initialization, final-flag mixing, and rounds-loop guard facts.
The implementation is split under `Fallback.Setup.Compression.*` to keep rebuilds smaller.
-/
