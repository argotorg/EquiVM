import Examples.Precompiles.Blake2f.Fallback.ZeroRounds.Words4To7

/-!
# BLAKE2F fallback zero-round output-loop traces

Aggregator for the zero-round output-loop setup and output-word facts. The implementation is split
under `Fallback.ZeroRounds.*` so output-loop word edits rebuild smaller units.
-/
