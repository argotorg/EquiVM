import Examples.Precompiles.Blake2f.Fallback.OutputBridge.Slots.ReturnReadback

/-!
# BLAKE2F fallback output-buffer bridge slots

Aggregator for slot/output/return readback facts. The implementation is split under
`Fallback.OutputBridge.Slots.*` so rebuilds can avoid rechecking one large bridge module.
-/
