import Benchmarks.Dss.End.FileAddress

/- This module is kept so the benchmark library can still build every source file.
The maintained `endFileAddress*` implementation lives in `Benchmarks.Dss.End.FileAddress`. -/
