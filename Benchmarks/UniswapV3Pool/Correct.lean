import Benchmarks.UniswapV3Pool.Constructor
import Benchmarks.UniswapV3Pool.Functions
import Solm.Equiv

/-!
# UniswapV3Pool benchmark correctness stub

Parameterized over the pool's immutable values `v`.  For each `v`, the deployed runtime is the
template patched with `v` (`patchRuntime uniswapV3PoolBytecode (patches v) = some code`), and runtime
equivalence is stated against `contract v` (whose immutable getters return `v`'s values).  The
whole-contract bundle pairs this with the parameterized constructor target.  All proofs are targets.
-/

open Solm ABI Ethereum Ethereum.EVM Benchmarks.UniswapV3Pool.Immutables

namespace Benchmarks.UniswapV3Pool

theorem uniswapV3PoolCorrect (v : PoolImmutables) {code : ByteArray}
    (hcode : patchRuntime uniswapV3PoolBytecode (patches v) = some code) :
    runtimeEquivalence (config v) code (contract v) := by
  constructor
  intro cA gh bl σ_evm σ_solm σ₀ g A I hIcode hsize hperm haccounts
  by_cases hwv : I.weiValue = ⟨0⟩
  · by_cases hshort : I.calldata.size < 4
    · exact uniswapV3PoolShortRevert (v := v) (cA := cA) (gh := gh) (bl := bl)
        (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀) (A := A) (I := I)
        (g := g) (hpatch := hcode) (hcode := hIcode) (_hsize := hsize)
        (_hperm := hperm) (hwv := hwv) (hsz := hshort)
    · have hsz : 4 ≤ I.calldata.size := by omega
      by_cases hnm :
          ∀ i, i < 26 → (uniswapV3PoolSelBytes i == I.calldata.extract 0 4) = false
      · exact uniswapV3PoolNoDispatch (v := v) (cA := cA) (gh := gh) (bl := bl)
          (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀) (A := A) (I := I)
          (g := g) (hpatch := hcode) (hcode := hIcode) (hwv := hwv) (hsz := hsz)
          (hsize := hsize) (_hperm := hperm) (hnm := hnm)
      · exact uniswapV3PoolSelectorMatchCases I hnm
          (fun hsel => uniswapV3PoolToken0BodyCore (v := v) (cA := cA) (gh := gh)
            (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀) (A := A)
            (I := I) (g := g) (hpatch := hcode) (hcode := hIcode) (hwv := hwv)
            (hsz := hsz) (hsize := hsize) (_hperm := hperm) (hAccounts := haccounts)
            (hsel := hsel))
          (fun hsel => uniswapV3PoolSwapBodyCore (v := v) (cA := cA) (gh := gh)
            (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀) (A := A)
            (I := I) (g := g) (hpatch := hcode) (hcode := hIcode) (hwv := hwv)
            (hsz := hsz) (hsize := hsize) (_hperm := hperm) (hAccounts := haccounts)
            (hsel := hsel))
          (fun hsel => uniswapV3PoolLiquidityBodyCore (v := v) (cA := cA) (gh := gh)
            (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀) (A := A)
            (I := I) (g := g) (hpatch := hcode) (hcode := hIcode) (hwv := hwv)
            (hsz := hsz) (hsize := hsize) (_hperm := hperm) (hAccounts := haccounts)
            (hsel := hsel))
          (fun hsel => uniswapV3PoolProtocolFeesBodyCore (v := v) (cA := cA) (gh := gh)
            (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀) (A := A)
            (I := I) (g := g) (hpatch := hcode) (hcode := hIcode) (hwv := hwv)
            (hsz := hsz) (hsize := hsize) (_hperm := hperm) (hAccounts := haccounts)
            (hsel := hsel))
          (fun hsel => uniswapV3PoolObservationsBodyCore (v := v) (cA := cA) (gh := gh)
            (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀) (A := A)
            (I := I) (g := g) (hpatch := hcode) (hcode := hIcode) (hwv := hwv)
            (hsz := hsz) (hsize := hsize) (_hperm := hperm) (hAccounts := haccounts)
            (hsel := hsel))
          (fun hsel => uniswapV3PoolIncreaseObservationCardinalityNextBodyCore (v := v)
            (cA := cA) (gh := gh) (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm)
            (σ₀ := σ₀) (A := A) (I := I) (g := g) (hpatch := hcode)
            (hcode := hIcode) (hwv := hwv) (hsz := hsz) (hsize := hsize)
            (_hperm := hperm) (hAccounts := haccounts) (hsel := hsel))
          (fun hsel => uniswapV3PoolSlot0BodyCore (v := v) (cA := cA) (gh := gh)
            (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀) (A := A)
            (I := I) (g := g) (hpatch := hcode) (hcode := hIcode) (hwv := hwv)
            (hsz := hsz) (hsize := hsize) (_hperm := hperm) (hAccounts := haccounts)
            (hsel := hsel))
          (fun hsel => uniswapV3PoolMintBodyCore (v := v) (cA := cA) (gh := gh)
            (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀) (A := A)
            (I := I) (g := g) (hpatch := hcode) (hcode := hIcode) (hwv := hwv)
            (hsz := hsz) (hsize := hsize) (_hperm := hperm) (hAccounts := haccounts)
            (hsel := hsel))
            (fun hsel => uniswapV3PoolFeeGrowthGlobal1X128BodyCore (v := v) (cA := cA)
            (gh := gh) (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀)
            (A := A) (I := I) (g := g) (hpatch := hcode) (hcode := hIcode)
            (hwv := hwv) (hsz := hsz) (hsize := hsize) (_hperm := hperm)
            (hAccounts := haccounts) (hsel := hsel))
          (fun hsel => uniswapV3PoolFlashBodyCore (v := v) (cA := cA) (gh := gh)
            (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀) (A := A)
            (I := I) (g := g) (hpatch := hcode) (hcode := hIcode) (hwv := hwv)
            (hsz := hsz) (hsize := hsize) (_hperm := hperm) (hAccounts := haccounts)
            (hsel := hsel))
          (fun hsel => uniswapV3PoolCollectBodyCore (v := v) (cA := cA) (gh := gh)
            (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀) (A := A)
            (I := I) (g := g) (hpatch := hcode) (hcode := hIcode) (hwv := hwv)
            (hsz := hsz) (hsize := hsize) (_hperm := hperm) (hAccounts := haccounts)
            (hsel := hsel))
          (fun hsel => uniswapV3PoolPositionsBodyCore (v := v) (cA := cA) (gh := gh)
            (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀) (A := A)
            (I := I) (g := g) (hpatch := hcode) (hcode := hIcode) (hwv := hwv)
            (hsz := hsz) (hsize := hsize) (_hperm := hperm) (hAccounts := haccounts)
            (hsel := hsel))
          (fun hsel => uniswapV3PoolTickBitmapBodyCore (v := v) (cA := cA) (gh := gh)
            (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀) (A := A)
            (I := I) (g := g) (hpatch := hcode) (hcode := hIcode) (hwv := hwv)
            (hsz := hsz) (hsize := hsize) (_hperm := hperm) (hAccounts := haccounts)
            (hsel := hsel))
          (fun hsel => uniswapV3PoolMaxLiquidityPerTickBodyCore (v := v) (cA := cA)
            (gh := gh) (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀)
            (A := A) (I := I) (g := g) (hpatch := hcode) (hcode := hIcode)
            (hwv := hwv) (hsz := hsz) (hsize := hsize) (_hperm := hperm)
            (hAccounts := haccounts) (hsel := hsel))
          (fun hsel => uniswapV3PoolSetFeeProtocolBodyCore (v := v) (cA := cA)
            (gh := gh) (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀)
            (A := A) (I := I) (g := g) (hpatch := hcode) (hcode := hIcode)
            (hwv := hwv) (hsz := hsz) (hsize := hsize) (_hperm := hperm)
            (hAccounts := haccounts) (hsel := hsel))
          (fun hsel => uniswapV3PoolCollectProtocolBodyCore (v := v) (cA := cA)
            (gh := gh) (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀)
            (A := A) (I := I) (g := g) (hpatch := hcode) (hcode := hIcode)
            (hwv := hwv) (hsz := hsz) (hsize := hsize) (_hperm := hperm)
            (hAccounts := haccounts) (hsel := hsel))
          (fun hsel => uniswapV3PoolObserveBodyCore (v := v) (cA := cA) (gh := gh)
            (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀) (A := A)
            (I := I) (g := g) (hpatch := hcode) (hcode := hIcode) (hwv := hwv)
            (hsz := hsz) (hsize := hsize) (_hperm := hperm) (hAccounts := haccounts)
            (hsel := hsel))
          (fun hsel => uniswapV3PoolBurnBodyCore (v := v) (cA := cA) (gh := gh)
            (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀) (A := A)
            (I := I) (g := g) (hpatch := hcode) (hcode := hIcode) (hwv := hwv)
            (hsz := hsz) (hsize := hsize) (_hperm := hperm) (hAccounts := haccounts)
            (hsel := hsel))
          (fun hsel => uniswapV3PoolSnapshotCumulativesInsideBodyCore (v := v)
            (cA := cA) (gh := gh) (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm)
            (σ₀ := σ₀) (A := A) (I := I) (g := g) (hpatch := hcode)
            (hcode := hIcode) (hwv := hwv) (hsz := hsz) (hsize := hsize)
            (_hperm := hperm) (hAccounts := haccounts) (hsel := hsel))
          (fun hsel => uniswapV3PoolFactoryBodyCore (v := v) (cA := cA) (gh := gh)
            (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀) (A := A)
            (I := I) (g := g) (hpatch := hcode) (hcode := hIcode) (hwv := hwv)
            (hsz := hsz) (hsize := hsize) (_hperm := hperm) (hAccounts := haccounts)
            (hsel := hsel))
          (fun hsel => uniswapV3PoolTickSpacingBodyCore (v := v) (cA := cA)
            (gh := gh) (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀)
            (A := A) (I := I) (g := g) (hpatch := hcode) (hcode := hIcode)
            (hwv := hwv) (hsz := hsz) (hsize := hsize) (_hperm := hperm)
            (hAccounts := haccounts) (hsel := hsel))
          (fun hsel => uniswapV3PoolToken1BodyCore (v := v) (cA := cA) (gh := gh)
            (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀) (A := A)
            (I := I) (g := g) (hpatch := hcode) (hcode := hIcode) (hwv := hwv)
            (hsz := hsz) (hsize := hsize) (_hperm := hperm) (hAccounts := haccounts)
            (hsel := hsel))
          (fun hsel => uniswapV3PoolFeeBodyCore (v := v) (cA := cA) (gh := gh)
            (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀) (A := A)
            (I := I) (g := g) (hpatch := hcode) (hcode := hIcode) (hwv := hwv)
            (hsz := hsz) (hsize := hsize) (_hperm := hperm) (hAccounts := haccounts)
            (hsel := hsel))
          (fun hsel => uniswapV3PoolFeeGrowthGlobal0X128BodyCore (v := v) (cA := cA)
            (gh := gh) (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀)
            (A := A) (I := I) (g := g) (hpatch := hcode) (hcode := hIcode)
            (hwv := hwv) (hsz := hsz) (hsize := hsize) (_hperm := hperm)
            (hAccounts := haccounts) (hsel := hsel))
          (fun hsel => uniswapV3PoolTicksBodyCore (v := v) (cA := cA) (gh := gh)
            (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀) (A := A)
            (I := I) (g := g) (hpatch := hcode) (hcode := hIcode) (hwv := hwv)
            (hsz := hsz) (hsize := hsize) (_hperm := hperm) (hAccounts := haccounts)
            (hsel := hsel))
          (fun hsel => uniswapV3PoolInitializeBodyCore (v := v) (cA := cA) (gh := gh)
            (bl := bl) (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀) (A := A)
            (I := I) (g := g) (hpatch := hcode) (hcode := hIcode) (hwv := hwv)
            (hsz := hsz) (hsize := hsize) (_hperm := hperm) (hAccounts := haccounts)
            (hsel := hsel))
  · exact uniswapV3PoolNonPayable (v := v) (cA := cA) (gh := gh) (bl := bl)
      (σ_evm := σ_evm) (σ_solm := σ_solm) (σ₀ := σ₀) (A := A) (I := I) (g := g)
      (hpatch := hcode) (hcode := hIcode) (hwv := hwv)

theorem uniswapV3PoolContractCorrect (v : PoolImmutables) {code : ByteArray}
    (hcode : patchRuntime uniswapV3PoolBytecode (patches v) = some code) :
    contractEquivalenceWith (config v) uniswapV3PoolCreationBytecode code (contract v)
      (runtimeCodeOf uniswapV3PoolBytecode) :=
  contractEquivalenceWith.intro (uniswapV3PoolConstructorCorrect v) (uniswapV3PoolCorrect v hcode)

end Benchmarks.UniswapV3Pool
